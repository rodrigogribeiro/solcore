contract Peano {
  data Nat = Zero | Succ[Nat]
  data Bool = True | False 

  function natInd (f, g, n) {
    match n {
    | Zero => return f(Zero) ;
    | Succ[m] => return g(natInd(f,g,m),m);
    };
  }
}
