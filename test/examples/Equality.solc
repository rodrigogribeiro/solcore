contract Equality {
   class a:Eq {
      function isEqual (x1 : a, x2 : a) -> Bool ; 
   }

   instance Word : Eq {
      function isEqual (w1 : Word, w2 : Word) -> Bool {
         primWordEq(w1, w2) ;
      }
   }
} 
