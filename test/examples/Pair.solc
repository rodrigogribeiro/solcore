contract Pair [a,b] {
  fst : a ;
  snd : b ;

  constructor (x,y) {
    fst = x;
    snd = y;
  }

  function setFst (x) {
    fst = x;
  }

  function getFst() {
    return fst;
  }

  function getSnd() {
    return snd;
  }
}
